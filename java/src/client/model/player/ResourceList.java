package client.model.player;

public class ResourceList {
	
	/**
	 * checks if resource list contains at least all of the resources present in the inputList
	 * @param inputList resourceList to compare this resource list against, inputList must be non-empty
	 * @return true if all resource amounts found in inputList are found in this resource list
	 */
	public boolean hasResource(ResourceList inputList){return true;}
}
